# Development & test dependencies. Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
httpx>=0.27
reportlab>=4.0     # generate sample PDFs / images in tests and samples/
