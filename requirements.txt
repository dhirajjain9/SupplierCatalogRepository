# Runtime dependencies (also used by the Vercel serverless function).
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
psycopg[binary]>=3.1   # PostgreSQL driver (shared storage on Vercel)
pydantic>=2.6
python-multipart>=0.0.9
anthropic>=0.40   # Claude provider (translation, taxonomy, vision)
openai>=1.40      # OpenAI provider — used when OPENAI_API_KEY is set
# Catalog import parsing
openpyxl>=3.1      # .xlsx
pdfplumber>=0.11   # .pdf table extraction
Pillow>=10.0       # image handling (also a pdfplumber dependency)
